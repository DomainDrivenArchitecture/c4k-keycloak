apiVersion: v1
kind: Service
metadata:
  name: keycloak
  labels:
    service: keycloak
  namespace: NAMESPACE
spec:
  ports:
  - name: "http"
    port: 80
    targetPort: 8080
  selector:
    app: keycloak